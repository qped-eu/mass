{
	"name": "qped-github-pages",
	"version": "1.0",
	"private": true,
	"homepage": "https://qped.github.io/",
	"scripts": {
		"predeploy": "npm run build",
		"deploy": "gh-pages -d build",
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
	},
	"eslintConfig": {
		"extends": "react-app"
	}
}